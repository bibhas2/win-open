This program tries to replicate the ``open`` command in mac or Ubuntu. It launches the program associated with a file.

Usage:

```
open file_name
```


I usually use the Bash shell that comes with Git for Windows. This doesn't include the ``open`` command. This program fills that gap.
